{
	"processors": [{
		"type": "image",
		"tensorflow_url": "localhost:8080"
	}],
	"outputs": [{
			"type": "elasticsearch",
			"user": "user",
			"password": "pass",
			"host": "http://localhost:9200",
			"index": "crawl-01"
		}
	]
}
